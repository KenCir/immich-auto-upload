<ContentDialog x:Class="ImmichDesktopUploader.Views.SettingsDialog"
 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
 Title="Settings" PrimaryButtonText="Save" CloseButtonText="Cancel" DefaultButton="Primary" PrimaryButtonClick="OnSave" Closed="OnClosed">
 <ContentControl IsEnabled="{Binding CanEdit}"><StackPanel Spacing="14" Width="440">
  <TextBox Header="Server URL" Text="{Binding ServerUrl, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" PlaceholderText="https://immich.example.com/api"/>
  <TextBlock Text="{Binding CredentialText}" TextWrapping="Wrap"/>
  <PasswordBox x:Name="NewKey" Header="New API Key" PasswordRevealMode="Hidden" PasswordChanged="OnPasswordChanged" AutomationProperties.AutomationId="ApiKeyInput"/>
  <TextBlock Text="空欄は既存キーを維持します。サーバーURLを変更する場合は新しいキーが必要です。" TextWrapping="Wrap"/>
  <CheckBox Content="Start with Windows" IsChecked="{Binding StartWithWindows, Mode=TwoWay}" AutomationProperties.AutomationId="StartWithWindows"/>
  <TextBlock Text="{Binding StartupStateText}" TextWrapping="Wrap"/>
  <TextBlock Text="{Binding Error}" TextWrapping="Wrap" Foreground="{ThemeResource SystemFillColorCriticalBrush}"/>
  <ProgressRing IsActive="{Binding SaveCommand.IsBusy}" Width="24" Height="24"/>
 </StackPanel></ContentControl>
</ContentDialog>
